pub fn tuple() {
    let data = (1, 1.9, true);

    //let (a, b, c) = data;
    let a = data.0;
    let b = data.1;
    let c = data.2;

    //let (a, b, c) = data;
    print!("{} {} {}", a, b ,c);
}

