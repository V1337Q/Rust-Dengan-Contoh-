pub fn arayy_indexing() {
    let gitu = [1, 5, 7];

    //let (a, b, c) = gitu;
    let a = gitu[0];
    let b = gitu[1];
    let c = gitu[2];

    //let (a, b, c) = gitu;
    print!("{} {} {}", a, b ,c);
}

